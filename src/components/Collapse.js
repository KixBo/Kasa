import "../styles/Collapse.scss";

function Collapse(props) {
  // state (état, données)
  
  // comportements
  
  // affichage (render)
    return (
      <>
      
        <div>

        </div>
      </>
    );
  }
  
  export default Collapse;