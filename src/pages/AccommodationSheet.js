function AccommodationSheet() {
// state (état, données)

// comportements

// affichage (render)
  return (
    <div>
      <p>Ceci est la page Fiche de Logement</p>
    </div>
  );
}

export default AccommodationSheet;
